<% if(pagina === 'login') { %>
    <link rel="stylesheet" href="/assets/css/contents/login.css">
<% } else { %>
    <link rel="stylesheet" href="/assets/css/partials/header-user.css">
    <link rel="stylesheet" href="/assets/css/partials/sections-header.css">
<% } %>

<% if(pagina === 'acerca') { %>
    <link rel="stylesheet" href="/assets/css/contents/acerca.css">
<% } %>

<% if(pagina === 'usertickets') { %>
    <link rel="stylesheet" href="/assets/css/contents/user-tickets.css">
<% } %>

<% if(pagina === 'solicitud') { %>
    <link rel="stylesheet" href="/assets/css/contents/solicitud.css">
<% } %>

<% if(pagina === 'perfil') { %>
    <link rel="stylesheet" href="/assets/css/contents/perfil.css">
<% } %>

<% if(pagina === 'verticket') { %>
    <link rel="stylesheet" href="/assets/css/contents/ver-ticket.css">
<% } %>

<!-- CSS de Admin -->

<% if(pagina === 'admintickets') { %>
    <link rel="stylesheet" href="/assets/css/contents/admin-tickets.css">
<% } %>