<% if(pagina === 'login') { %>
    <%-include('../partials/header-index.ejs')%>
    <%-include('../contents/login.ejs')%>
<% } %>

<% if(pagina === 'usertickets') { %>
    <%-include('../partials/header-home.ejs')%>
    <%-include('../partials/section-header.ejs')%>
    <%-include('../contents/user-tickets.ejs')%>
<% } %>

<% if(pagina === 'solicitud') { %>
    <%-include('../partials/header-home.ejs')%>
    <%-include('../partials/section-header.ejs')%>
    <%-include('../contents/solicitud.ejs')%>
<% } %>

<% if(pagina === 'perfil') { %>
    <%-include('../partials/header-home.ejs')%>
    <%-include('../partials/section-header.ejs')%>
    <%-include('../contents/perfil.ejs')%>
<% } %>