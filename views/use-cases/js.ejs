<% if(pagina === 'perfil' || pagina === 'verperfil') { %>
    <script type="module" src="assets/js/back.js"></script>
<% } %>

<% if(pagina === 'solicitud' || pagina === 'editar') { %>
    <script type="module" src="assets/js/solicitud.js"></script>
<% } %>

<% if(pagina === 'verticket') { %>
    <script type="module" src="assets/js/verticket.js"></script>
<% } %>

<!-- JS del admin -->

<% if(pagina === 'admintickets') { %>
    <script type="module" src="assets/js/admin-tickets.js"></script>
<% } %>

<% if(pagina === 'usuarios') { %>
    <script type="module" src="assets/js/usuarios.js"></script>
<% } %>

<% if(pagina === 'editarusuario' || pagina === 'crearusuario') { %>
    <script type="module" src="assets/js/editar-img.js"></script>
<% } %>