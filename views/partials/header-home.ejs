<header>
    <div class="container">
        <figure class="logo__figure">
            <img src="/assets/imgs/logo.PNG" class="logo__img">
            <p><span>S</span>Ticket</p>
            <button class="btn menu"><i class="bi bi-list"></i></button>
        </figure>
        <div class="buttons">
            <div class="logo__content">
                <figure class="logo__figure">
                    <img src="/assets/imgs/logo.PNG" class="logo__img">
                    <p><span>S</span>Ticket</p>
                </figure>
                <button class="exit"><i class="bi bi-x"></i></button>
            </div>
            <% if(user.isAdmin) { %>
                <a class="btn <%= pagina === 'admintickets' ? 'selected' : '' %>" href="/admintickets">Inicio</a>
                <a class="btn <%= pagina === 'usuarios' ? 'selected' : '' %>" href="/usuarios">Usuarios</a>
            <% } else { %>
                <a class="btn <%= pagina === 'usertickets' ? 'selected' : '' %>" href="/home">Inicio</a>
            <% } %>
            <a class="btn <%= pagina === 'acceso' ? 'selected' : '' %>" href="/acceso">Acceso</a>
        </div>
        <div class="perfil__content">
            <div class="perfil">
                <img src="/assets/imgs/<%= user.foto ? 'perfil/' + user.foto : 'user-default.png' %>" onerror="this.onerror=null; this.src='/assets/imgs/user-default.png';">
                <form class="opciones" action="/logout?_method=DELETE" method="POST">
                    <a class="opcion" href="/perfil"><i class="bi bi-person"></i> Perfil</a>
                    <button class="opcion" type="submit"><i class="bi bi-box-arrow-left"></i> Cerrar Sesión</button>
                </form>
            </div>
            <p>@<%= user.usuario %></p>
        </div>
    </div>
</header>